import { ThemeColor } from '@components/Button/types';
import { Colors } from '@constants/colors';

export type ColorsTypes =
  // add new colors here
  'transparent' | 'red' | 'white' | 'gray' | 'black' | ThemeColor ;
